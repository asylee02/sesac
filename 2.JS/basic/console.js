const a ='20';
const b = 10;

// js는 약타입이기 때문에, 자동 타입 변환이 가능하며 다른 타입들도 연산이 됨

console.log(a+b); // 자동으로 숫자가 문자열로 변환되며 연산됨


const username = 'alice';
const age = 20;

console.log(`name: ${username}, Age : ${age}`)

